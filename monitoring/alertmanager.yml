global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'severity']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: 'siem-webhook'
  routes:
    - match:
        severity: critical
      receiver: 'siem-webhook'
      repeat_interval: 1h

receivers:
  - name: 'siem-webhook'
    webhook_configs:
      - url: 'http://backend:8000/api/health'
        send_resolved: true

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname']
